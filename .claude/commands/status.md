---
description: Resume o estado atual do TaskFloww (PROJECT_STATUS.md + git status) sem propor mudanças
---

Leia `/docker/taskfloww/PROJECT_STATUS.md` e rode `git status` e `git log --oneline -10`. Apresente um resumo curto: fase atual do projeto, o que está implementado, o que está em andamento no branch atual (arquivos modificados/novos/deletados), e qualquer divergência óbvia entre o que `PROJECT_STATUS.md`/`CLAUDE.md` descrevem e o que o código/git status realmente mostram (ex.: documento dizendo "só mock" quando já há migrações reais). Não proponha alterações nos documentos nesta análise — apenas relate.
