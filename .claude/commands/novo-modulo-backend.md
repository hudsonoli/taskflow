---
description: Cria um novo módulo de backend do TaskFloww (model, schema, repository, service, routes, migração Alembic)
---

Invoque a skill `migracao-backend` para criar o módulo de backend "$ARGUMENTS". Siga o processo da skill à risca: consulte o CLAUDE.md (regras de modelagem) e o Graphify antes de qualquer geração de código, apresente o plano de arquivos e colunas, e só escreva código após aprovação explícita do usuário. Nunca rode `alembic upgrade` automaticamente.
